// Chrome copy for EN / ID — operator-facing workflow language.

export global Strings {
    in-out property <string> locale: "en";
    out property <bool> is-id: locale == "id";

    out property <string> brand-product: "Acquire";
    out property <string> evidence-station: is-id ? "Meja akuisisi" : "Acquisition bench";
    out property <string> operator-mode: is-id ? "MODE" : "MODE";
    out property <string> mode-guided: is-id ? "Terpandu" : "Guided";
    out property <string> mode-standard: is-id ? "Standar" : "Standard";
    out property <string> mode-expert: is-id ? "Ahli" : "Expert";
    out property <string> hint-guided: is-id ? "Demo cepat" : "Quick demo";
    out property <string> hint-standard: is-id ? "Pilih berkas" : "Pick files";
    out property <string> hint-expert: is-id ? "Raw / allowlist" : "Raw / allowlist";
    out property <string> lab-only: is-id ? "HANYA LAB" : "LAB USE ONLY";
    out property <string> alpha-line: is-id ? "Alpha v0.1 · GPLv3" : "Alpha v0.1 · GPLv3";
    out property <string> founder-badge: "UNSIGNED";
    out property <string> unsigned-banner: is-id
        ? "BELUM DITANDATANGANI — hash segel bukan tanda tangan digital."
        : "UNSIGNED — hash seal is not a digital signature.";
    out property <string> disclosure-banner: is-id
        ? "⚠ Hanya lab · BUKAN siap pengadilan · BUKAN bersertifikat ISO"
        : "⚠ Lab use only · NOT court-ready · NOT ISO-certified";
    out property <string> search-placeholder: is-id ? "Cari menu…" : "Search…";
    out property <string> status-evidence: is-id ? "Evidence" : "Evidence";
    out property <string> status-coverage: is-id ? "Coverage" : "Coverage";
    out property <string> status-extra: is-id ? "Extra" : "Extra";
    out property <string> acquire-mode-label: is-id ? "acquire-mode" : "acquire-mode";
    out property <string> stage-prepare: is-id ? "PREPARE" : "PREPARE";
    out property <string> stage-acquire: is-id ? "ACQUIRE" : "ACQUIRE";
    out property <string> stage-seal: is-id ? "SEAL" : "SEAL";
    out property <string> zone-source: is-id ? "SUMBER" : "SOURCE";
    out property <string> zone-prepare: is-id ? "SIAPKAN" : "PREPARE";
    out property <string> zone-run: is-id ? "JALANKAN / SEGEL" : "RUN / SEAL";
    out property <string> no-package-yet: is-id ? "Paket belum dibuat" : "Package not yet generated";
    out property <string> start-acquire: is-id ? "START ACQUIRE" : "START ACQUIRE";
    out property <string> advanced-toggle: is-id ? "Advanced" : "Advanced";
    out property <string> advanced-hide: is-id ? "Hide advanced" : "Hide advanced";
    out property <string> shell-about-title: is-id ? "Tentang" : "About";
    out property <string> hash-seal-not-signature: is-id
        ? "Hash segel bukan tanda tangan digital."
        : "A hash seal is not a digital signature.";
    out property <string> about-build: is-id ? "Identitas build" : "Build identity";
    out property <string> about-product: is-id ? "Meja akuisisi forensik" : "Forensic acquisition bench";
    out property <string> about-version-label: is-id ? "Versi" : "Version";
    out property <string> empty-disks-title: is-id ? "Tidak ada disk terhubung." : "No disks connected.";
    out property <string> empty-disks-body: is-id ? "Hubungkan perangkat keras untuk mulai." : "Connect hardware to start.";
    out property <string> device-details: is-id ? "DETAIL PERANGKAT" : "DEVICE DETAILS";
    out property <string> detail-interface: is-id ? "Interface" : "Interface";
    out property <string> detail-sectors: is-id ? "Sectors" : "Sectors";
    out property <string> detail-sector-size: is-id ? "Sector size" : "Sector size";
    out property <string> detail-partitions: is-id ? "Partitions" : "Partitions";
    out property <string> detail-type: is-id ? "Type" : "Type";
    out property <string> detail-readonly: is-id ? "Read-only" : "Read-only";
    out property <string> path-source: is-id ? "SOURCE PATH" : "SOURCE PATH";
    out property <string> path-output: is-id ? "OUTPUT PATH" : "OUTPUT PATH";
    out property <string> case-verify-label: is-id ? "EVIDENCE / CASE ID" : "EVIDENCE / CASE ID";
    out property <string> case-verify-placeholder: is-id ? "Harus cocok dengan ID kasus" : "Matches case ID";
    out property <string> hw-disks: is-id ? "Disk" : "Disks";
    out property <string> hw-blocker: is-id ? "Blocker" : "Blocker";
    out property <string> hw-integrity: is-id ? "Integrity" : "Integrity";
    out property <string> mode-in-header: is-id ? "Mode" : "Mode";
    out property <string> court-path-a: is-id ? "Path A kandidat — RAW + .fsnap" : "Path A candidate — RAW + .fsnap";
    out property <string> experimental-not-applied: is-id
        ? "Experimental — kontrol di bawah belum diterapkan ke AcquireRequest."
        : "Experimental — controls below are not applied to AcquireRequest.";
    out property <string> col-dev: "DEV";
    out property <string> col-model: is-id ? "MODEL" : "MODEL";
    out property <string> col-size: is-id ? "UKURAN" : "SIZE";
    out property <string> col-bus: "BUS";
    out property <string> case-verify-mismatch: is-id
        ? "Case verify tidak cocok dengan Case ID."
        : "Case verify does not match case ID.";
    out property <string> identify-gate-banner: is-id
        ? "Identify belum lengkap — checklist wajib sebelum akuisisi Expert/blockish."
        : "Identify gate incomplete — checklist required before Expert/blockish acquire.";
    out property <string> open-identify: is-id ? "Buka Identify" : "Open Identify";
    out property <string> create-case-heading: is-id ? "BUAT KASUS" : "CREATE CASE";
    out property <string> case-title-ph: is-id ? "Judul kasus" : "Case title";
    out property <string> examiner-ph: is-id ? "Pemeriksa" : "Examiner";
    out property <string> continue-identify: is-id ? "Lanjut ke Identify" : "Continue to Identify";
    out property <string> open-acquire: is-id ? "Buka Acquire" : "Open Acquire";
    out property <string> case-list: is-id ? "DAFTAR KASUS" : "CASE LIST";
    out property <string> id-power: is-id ? "Daya terisolasi / stabil" : "Power isolated / stable";
    out property <string> id-network: is-id ? "Jaringan terisolasi" : "Network isolated";
    out property <string> id-encryption: is-id ? "Enkripsi dicatat" : "Encryption noted";
    out property <string> id-anti: is-id ? "Anti-forensik dipertimbangkan" : "Anti-forensics considered";
    out property <string> id-oov: is-id ? "Out-of-scope diakui (OoV)" : "Out-of-scope acknowledged (OoV)";
    out property <string> emergency-override-label: is-id ? "Override darurat (opsional)" : "Emergency override (optional)";
    out property <string> emergency-override-ph: is-id ? "Alasan supervisor — diaudit" : "Supervisor reason — audited";
    out property <string> continue-acquire: is-id ? "Lanjut ke Acquire" : "Continue to Acquire";
    out property <string> last-package: is-id ? "PAKET TERAKHIR" : "LAST PACKAGE";
    out property <string> tools-actions: is-id ? "AKSI" : "ACTIONS";
    out property <string> browse-package: is-id ? "Jelajah paket" : "Browse package";
    out property <string> compare-package-b-ph: is-id ? "Bandingkan paket B" : "Compare package B";
    out property <string> log-detail: is-id ? "LOG / DETAIL" : "LOG / DETAIL";
    out property <string> boot-image-ph: is-id ? "Jalur image boot" : "Boot image path";
    out property <string> settings-label: is-id ? "PENGATURAN" : "SETTINGS";
    out property <string> default-examiner-ph: is-id ? "Pemeriksa default" : "Default examiner";
    out property <string> operator-help: is-id ? "BANTUAN OPERATOR" : "OPERATOR HELP";
    out property <string> unavailable-probed: is-id ? "Tidak tersedia / belum diprobe" : "Unavailable / not probed";

    out property <string> rail-workflow: is-id ? "ALUR" : "WORKFLOW";
    out property <string> rail-lab: is-id ? "LAB" : "LAB";
    out property <string> rail-support: is-id ? "DUKUNGAN" : "SUPPORT";
    out property <string> nav-cases: is-id ? "Kasus" : "Cases";
    out property <string> hint-cases: is-id ? "Konteks operator" : "Operator context";
    out property <string> nav-identify: is-id ? "Identifikasi" : "Identify";
    out property <string> hint-identify: is-id ? "ISO · kenali sumber" : "ISO · know source";
    out property <string> nav-acquire: is-id ? "Akuisisi" : "Acquire";
    out property <string> hint-acquire: is-id ? "Siapkan · jalankan · segel" : "Prepare · run · seal";
    out property <string> nav-triage: is-id ? "Triage" : "Triage";
    out property <string> hint-triage: is-id ? "Pasca-akuisisi" : "Post-acquire";
    out property <string> nav-tools: is-id ? "Alat" : "Tools";
    out property <string> hint-tools: is-id ? "Verifikasi / utilitas" : "Verify / utilities";
    out property <string> nav-qms: "QMS";
    out property <string> hint-qms: is-id ? "Validasi lab" : "Lab validation";
    out property <string> nav-boot: is-id ? "Boot (preview)" : "Boot (preview)";
    out property <string> hint-boot: is-id ? "Media portabel" : "Portable media";
    out property <string> nav-help: is-id ? "Bantuan" : "Help";
    out property <string> hint-help: is-id ? "SOP singkat" : "Short SOP";
    out property <string> nav-about: is-id ? "Tentang" : "About";
    out property <string> hint-about: is-id ? "Identitas build" : "Build identity";

    out property <string> chip-case: is-id ? "Kasus" : "Case";
    out property <string> chip-wb: is-id ? "Write-blocker" : "Write-blocker";
    out property <string> chip-integrity: is-id ? "Integritas" : "Integrity";
    out property <string> integrity-standby: is-id ? "Siaga" : "Standby";
    out property <string> integrity-ready: is-id ? "Siap akuisisi" : "Ready";
    out property <string> integrity-sealed: is-id ? "Tersegel" : "Sealed";
    out property <string> integrity-running: is-id ? "Berjalan" : "Running";
    out property <string> case-id-label: is-id ? "ID BUKTI / KASUS" : "EVIDENCE / CASE ID";
    out property <string> examiner-label: is-id ? "PEMERIKSA" : "EXAMINER";
    out property <string> confirm-wb: is-id
        ? "Saya konfirmasi write-blocker perangkat keras terpasang (sumber blok)."
        : "I confirm a hardware write-blocker is in place (block device source).";

    out property <string> shell-cases-title: is-id ? "Konteks kasus" : "Case context";
    out property <string> shell-cases-body: is-id
        ? "Tetapkan identitas kasus dan pemeriksa sebelum akuisisi. ISO/IEC 27037: catatan siapa/kapan harus dapat diaudit bersama bukti."
        : "Set case and examiner identity before acquisition. ISO/IEC 27037: who/when must remain auditable with the evidence.";
    out property <string> shell-identify-title: is-id ? "Identifikasi sumber" : "Source identification";
    out property <string> shell-identify-body: is-id
        ? "Catat daya, jaringan, enkripsi, dan checklist di luar pandang. Jangan klaim write-blocker atau HPA tanpa bukti probe."
        : "Record power, network, encryption, and out-of-view checklist. Do not claim write-blocker or HPA without probe evidence.";
    out property <string> shell-triage-title: is-id ? "Triage baca-saja" : "Read-only triage";
    out property <string> shell-triage-body: is-id
        ? "Tinjau hasil setelah segel. Analisis mendalam (gaya Autopsy) bukan pekerjaan meja akuisisi — gunakan Lab terpisah."
        : "Review after seal. Deep analysis (Autopsy-style) is not the acquisition job — use a separate Lab surface.";
    out property <string> shell-tools-title: is-id ? "Alat verifikasi" : "Verification tools";
    out property <string> shell-tools-body: is-id
        ? "Hub verifikasi dan utilitas. Hash segel membuktikan konsistensi byte; verifikasi independen tetap wajib."
        : "Verifier hub and utilities. A hash seal proves byte consistency; independent verification remains required.";
    out property <string> shell-qms-title: is-id ? "Mutu lab (QMS)" : "Lab quality (QMS)";
    out property <string> shell-qms-body: is-id
        ? "Self-test, dataset dikenal, dan matriks validasi. Klaim produk harus sama dengan bukti yang tercatat."
        : "Self-test, known datasets, and validation matrix. Product claims must match recorded evidence.";
    out property <string> shell-boot-title: is-id ? "Media boot / portabel" : "Boot / portable media";
    out property <string> shell-boot-body: is-id
        ? "Paket USB dan distribusi portabel untuk akuisisi di lapangan. Tanda tangan/notarisasi produksi masih di luar jalur lab."
        : "USB pack and portable distribution for field acquisition. Production signing/notarization remains outside the lab track.";
    out property <string> shell-help-title: is-id ? "Bantuan operator" : "Operator help";
    out property <string> shell-help-body: is-id
        ? "SOP singkat: write-blocker, di luar validasi, segel vs tanda tangan, format, dan pintasan keyboard."
        : "Short SOP: write-blocker, out of validation, seal vs signature, formats, and keyboard shortcuts.";
    out property <string> help_wb: is-id
        ? "Write-blocker: gunakan blocker perangkat keras bila tersedia. Jika tidak, catat konfirmasi manual di audit. Jangan klaim terdeteksi tanpa bukti."
        : "Write-blocker: use a hardware blocker when available. Otherwise record manual confirmation in the audit. Do not claim detection without evidence.";
    out property <string> help_oov: is-id
        ? "Sumber di luar validasi: hentikan bila sumber, tujuan, atau metode tidak tercakup kebijakan lab. Buat prosedur baru sebelum melanjutkan."
        : "Out of validation: stop when the source, destination, or method is outside the lab policy. Create a new procedure before proceeding.";
    out property <string> help_seal_vs_sig: is-id
        ? "Segel hash membuktikan konsistensi byte yang diperiksa; bukan tanda tangan digital, otorisasi, atau pengganti verifikasi independen."
        : "A hash seal proves consistency of the checked bytes; it is not a digital signature, authorization, or a substitute for independent verification.";
    out property <string> help_formats: is-id
        ? "Format: .fsnap (Path A kandidat), E01 (ewf-image), ZFF opsional, Split-RAW. Catat verifikasi eksternal di EVIDENCE.md."
        : "Formats: .fsnap (Path A candidate), E01 (ewf-image), optional ZFF, Split-RAW. Record external verification in EVIDENCE.md.";
    out property <string> help_shortcuts: is-id
        ? "Pintasan: Tab memindahkan fokus, Space mengaktifkan kontrol, Esc membatalkan dialog. Gunakan fokus keyboard yang terlihat untuk meninjau sebelum akuisisi."
        : "Shortcuts: Tab moves focus, Space activates controls, Esc cancels dialogs. Use visible keyboard focus to review before acquisition.";

    out property <string> step-prepare: is-id ? "1 · Siapkan" : "1 · Prepare";
    out property <string> step-run: is-id ? "2 · Akuisisi" : "2 · Acquire";
    out property <string> step-seal: is-id ? "3 · Segel" : "3 · Seal";

    out property <string> prepare-guided-title: is-id ? "Siapkan sumber demo" : "Prepare a demo source";
    out property <string> prepare-guided-body: is-id
        ? "Isi jalur sintetis, konfirmasi, lalu akuisisi. Tidak menyentuh disk raw."
        : "Load synthetic paths, confirm, then acquire. Never touches raw disks.";
    out property <string> prepare-paths-title: is-id ? "Tetapkan sumber dan keluaran" : "Set source and output";
    out property <string> prepare-paths-body: is-id
        ? "Pilih berkas sumber dan folder paket. Konfirmasi bahwa ini pelatihan, bukan bukti nyata."
        : "Choose a source file and package folder. Confirm this is training, not real evidence.";

    out property <string> case-note: is-id ? "Catatan kasus (opsional)" : "Case note (optional)";
    out property <string> case-placeholder: is-id ? "Mis. TRN-2026-0001 — tidak masuk audit kriptografis" : "e.g. TRN-2026-0001 — outside the cryptographic audit";
    out property <string> paths: is-id ? "Jalur" : "Paths";
    out property <string> source: is-id ? "Sumber" : "Source";
    out property <string> output: is-id ? "Keluaran" : "Output";
    out property <string> source-placeholder: is-id ? "Berkas sumber sintetis" : "Synthetic source file";
    out property <string> output-placeholder: is-id ? "Direktori keluaran" : "Output directory";
    out property <string> browse: is-id ? "Jelajah" : "Browse";
    out property <string> confirm-synthetic: is-id ? "Saya konfirmasi: sumber sintetis / pelatihan — bukan bukti nyata." : "I confirm: synthetic / training source — not real evidence.";

    out property <string> acquire: is-id ? "Mulai akuisisi" : "Start acquire";
    out property <string> working: is-id ? "Mengakuisisi…" : "Acquiring…";
    out property <string> cancel: is-id ? "Batalkan" : "Cancel";
    out property <string> fill-demo: is-id ? "Isi demo sintetis" : "Load synthetic demo";
    out property <string> export-coc: is-id ? "Ekspor ringkasan CoC" : "Export CoC summary";
    out property <string> clear: is-id ? "Reset hasil" : "Reset results";
    out property <string> ready-hint: is-id ? "Siap diakuisisi" : "Ready to acquire";
    out property <string> need-confirm: is-id ? "Konfirmasi sumber dulu" : "Confirm the source first";

    out property <string> evidence-seal: is-id ? "SEGEL BUKTI" : "EVIDENCE SEAL";
    out property <string> verified: is-id ? "TERVERIFIKASI" : "VERIFIED";
    out property <string> awaiting: is-id ? "BELUM ADA" : "NO SEAL YET";
    out property <string> seal-empty-title: is-id ? "Paket belum dibuat" : "Package not yet generated";
    out property <string> seal-empty-body: is-id
        ? "Setelah akuisisi berhasil, SHA-256 dan paket .fsnap muncul di sini."
        : "After a successful acquire, the SHA-256 and .fsnap package appear here.";
    out property <string> sha-label: "SHA-256";
    out property <string> status: is-id ? "Status" : "Status";
    out property <string> size: is-id ? "Ukuran" : "Size";
    out property <string> package: is-id ? "Paket" : "Package";
    out property <string> none-label: is-id ? "—" : "—";
    out property <string> footer: is-id
        ? "Verifikasi independen wajib · batal menghentikan akuisisi · belum divalidasi untuk bukti produksi"
        : "Independent verify required · cancel stops in-flight acquire · not validated for production evidence";
    out property <string> theme-dark: is-id ? "Gelap" : "Dark";
    out property <string> theme-light: is-id ? "Terang" : "Light";
    out property <string> lang-en: "EN";
    out property <string> lang-id: "ID";
    out property <string> state-standby: is-id ? "Siaga" : "Standby";
    out property <string> state-running: is-id ? "Berjalan" : "Running";
    out property <string> state-sealed: is-id ? "Tersegel" : "Sealed";

    out property <string> wizard-case: is-id ? "Kasus" : "Case";
    out property <string> wizard-source: is-id ? "Sumber" : "Source";
    out property <string> wizard-wb: is-id ? "Write-blocker" : "Write-blocker";
    out property <string> wizard-confirm: is-id ? "Konfirmasi" : "Confirm";
    out property <string> wizard-start: is-id ? "Mulai" : "Start";
    out property <string> telemetry: is-id ? "TELEMETRI" : "TELEMETRY";
    out property <string> coverage-map: is-id ? "PETA CAKUPAN" : "COVERAGE MAP";
    out property <string> custody-timeline: is-id ? "LINIMASA CUSTODY" : "CUSTODY TIMELINE";
    out property <string> format-label: is-id ? "FORMAT KELUARAN" : "OUTPUT FORMAT";
    out property <string> disks-label: is-id ? "DISK HOST" : "HOST DISKS";
    out property <string> refresh-disks: is-id ? "Muat ulang disk" : "Refresh disks";
    out property <string> use-selected-disk: is-id ? "Pakai baris pertama sebagai sumber" : "Use first disk path as source";
    out property <string> create-case: is-id ? "Buat kasus" : "Create case";
    out property <string> refresh-cases: is-id ? "Muat ulang kasus" : "Refresh cases";
    out property <string> save-identify: is-id ? "Simpan identify" : "Save identify";
    out property <string> probe-encryption: is-id ? "Probe enkripsi" : "Probe encryption";
    out property <string> run-verify: is-id ? "Verifikasi paket" : "Verify package";
    out property <string> run-hash: is-id ? "Hash saja" : "Hash only";
    out property <string> run-qms: is-id ? "Jalankan QMS" : "Run QMS";
    out property <string> run-triage: is-id ? "Jalankan triage" : "Run triage";
    out property <string> boot-plan: is-id ? "Rencana boot (dry-run)" : "Boot plan (dry-run)";
    out property <string> analysis-lite: is-id ? "Analysis lite" : "Analysis lite";
    out property <string> sha512-label: "SHA-512";
    out property <string> write-sha512: is-id ? "Tulis sidecar SHA-512" : "Write SHA-512 sidecar";
    out property <string> segment-mib: is-id ? "Segmen MiB (split)" : "Segment MiB (split)";
    out property <string> retry-policy: is-id ? "Kebijakan bad-sector: lewati & catat (default lab)" : "Bad-sector policy: skip & log (lab default)";
    out property <string> retry-label: is-id ? "KEBIJAKAN BAD-SECTOR" : "BAD-SECTOR POLICY";
    out property <string> retry-skip: is-id ? "Lewati" : "Skip";
    out property <string> retry-n: is-id ? "Retry×3" : "Retry×3";
    out property <string> retry-fail: is-id ? "Fail-closed" : "Fail-closed";
    out property <string> profile-label: is-id ? "PROFIL" : "PROFILE";
    out property <string> profile-quick: "Quick";
    out property <string> profile-standard: "Standard";
    out property <string> profile-deep: "Deep";
    out property <string> profile-custom: "Custom";
    out property <string> run-quick-profile: is-id ? "Jalankan Quick (logical)" : "Run Quick (logical)";
    out property <string> source-kind-label: is-id ? "JENIS SUMBER" : "SOURCE KIND";
    out property <string> kind-disk: "Disk";
    out property <string> kind-file: is-id ? "Berkas" : "File";
    out property <string> kind-ram: "RAM";
    out property <string> kind-snap: "Snapshot";
    out property <string> coc-form-label: is-id ? "FORM CoC" : "CoC FORM";
    out property <string> coc-device: is-id ? "ID perangkat / media" : "Device / media ID";
    out property <string> coc-media: is-id ? "Media #" : "Media #";
    out property <string> coc-seq: is-id ? "Seq #" : "Seq #";
    out property <string> coc-description: is-id ? "Deskripsi bukti" : "Evidence description";
    out property <string> export-full-coc: is-id ? "Ekspor CoC + QR" : "Export CoC + QR";
    out property <string> compare-packages: is-id ? "Bandingkan" : "Compare";
    out property <string> browse-compare: is-id ? "Pilih paket B" : "Pick package B";
    out property <string> export-report: is-id ? "Ekspor bukti" : "Export evidence";
    out property <string> compare-hint: is-id ? "Pilih paket B untuk compare." : "Pick package B to compare.";
    out property <string> multisource-label: is-id ? "MULTISOURCE" : "MULTISOURCE";
    out property <string> multisource-b: is-id ? "Sumber B (path)" : "Source B (path)";
    out property <string> run-multisource: is-id ? "Jalankan dual-source" : "Run dual-source";
    out property <string> deny-title: is-id ? "Akuisisi ditolak" : "Acquire denied";
    out property <string> deny-body: is-id
        ? "Perbaiki jalur atau konfirmasi write-blocker, lalu coba lagi."
        : "Fix the path or write-blocker confirmation, then retry.";
}
