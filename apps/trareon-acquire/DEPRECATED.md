# Deprecated: Tauri + Svelte shell

**Status:** archived / not built.

Trareon Acquire’s desktop shell is **Slint** (`apps/acquire-slint`).
This directory is retained only as historical reference for the M0
foundation demo UI. It is **not** a workspace member and is not exercised
by CI.

Use:

```bash
cargo run -p acquire-slint --features gui
```

Decision: `docs/ai-operations/DECISIONS/2026-07-17-acquire-slint-gplv3.md`
